// TODO: Task 2.12 builds this screen. It can reuse `requestCameraStream`/
// `stopCameraStream` from `../../native/camera.ts` (built for Task 2.11's
// LabelReader) for the getUserMedia permission flow; `captureFrame` there is
// LabelReader's own one-shot-still concern and is not expected to fit a
// continuous BarcodeDetector read loop.
export function BarcodeScanner() {
  return null;
}
